<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 命名空间mapper,如果有多个mapper文件，这个必须唯一 -->
<mapper namespace="com.liyingjun.model">
    <select id="loginCase" parameterType="Integer" resultType="com.liyingjun.model.LoginCase">
        select * from `wx-test`.loginCase where id = #{id};
    </select>
</mapper>