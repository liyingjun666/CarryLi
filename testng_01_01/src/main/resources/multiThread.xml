<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd" >

<suite name="thread" parallel="methods" thread-count="3">

<!--    tests 级别 ：不同的test tag 下的用例可以在不同的线程下去执行
                        相同的test tag下的用例只能在同一个线程中去执行

        classes级别： 相同的class tag下的用例再同一个线程中去执行
                      不同的class tag下的用例可以在不同的线程中执行

        methods级别：所有用了都可以再不同的线程下去执行
        thread-count ：代表最大并发线程数
        xml文件配置这种方式不能指定线程池，只有方法上才可以指定线程池
    -->
    <test name="demo01">
        <classes >
            <class name="com.carry.multiThread.MultiThreadOnXml"/>
        </classes>
    </test>

    <test name="demo02">
        <classes >
            <class name="com.carry.multiThread.MultiThreadOnXml"></class>
        </classes>
    </test>
</suite>